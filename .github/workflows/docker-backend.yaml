name: Build Docker Image and push to GitHub

on:
  workflow_dispatch:
  push:
    tags:
     - backend*
     - frontend*

permissions:
  contents: write
  packages: write

env:
  IMAGE_NAME: ${{ github.repository_owner }}/cd-playground-backend

jobs:
  Docker-build-and-push:
    if: startsWith(GITHUB_REF.name,'backend')
    runs-on: ubuntu-latest
    steps:

      - name: checkout
        uses: actions/checkout@v4

      - name: docker login to ghcr.io
        uses: docker/login-action@v3
        with:
          #actor means owner of repo
          username: ${{ github.actor }}
          #password can be replaced with github token that is created at runtime
          password: ${{ secrets.GITHUB_TOKEN }}
          registry: ghcr.io

      - name: Extract tag name
        id: tag
        run: echo "TAG=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./src/backend
          push: true
          tags: |
            ghcr.io/${{ env.IMAGE_NAME }}:latest
